{
  "name": "web-music-player-dev",
  "version": "0.0.0",
  "private": true,
  "description": "Root task runner — one-command native dev environment (mprocs). The real projects live in backend/ and frontend/.",
  "packageManager": "pnpm@11.25.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "mprocs",
    "setup": "pnpm -C backend install && pnpm -C frontend install"
  },
  "devDependencies": {
    "mprocs": "^0.9.6"
  }
}
